
Xcodeをインストール後、次を実行

```
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
mkdir ~/Workspace
cd ~/Workspace
git clone git@github.com:53ningen/config.git
./config/osx/install.sh
```

